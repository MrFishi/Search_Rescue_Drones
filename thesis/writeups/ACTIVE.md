# Active write-up

sem1_proposal_lit_review

---

Change this file to switch which write-up the skills (`/outline`, `/evaluate`)
read the rubric and exemplars from. Only one active write-up at a time.

Add a new row here — and a new folder under `thesis/writeups/` — each time you
start the next write-up. Don't pre-create folders for stages you haven't reached.

## Write-ups

| Folder | Status | Notes |
|---|---|---|
| `sem1_progress_report` | complete (submitted) | Kept for continuity — later write-ups read its `final_submission/`, not a rubric (none needed here, already graded). |
| `sem1_proposal_lit_review` | **active** | Working on this now. Rubric + template in `rubric_and_template/`, a strong past example in `exemplars/`. |