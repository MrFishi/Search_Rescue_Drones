# Literature Review — Gap-Coverage Report (run 1)

> Generated by `/lit-review`, 2026-09-16. Sourcing report only, no prose drafted.
> Re-run after each `/find-refs` pass and compare against this file.
>
> **Scanned:** 22 project `.md` files (all of `thesis_docs/`, `training_nomenclature.md`,
> READMEs, every `PROVENANCE.md`, `overfit_gates.md`, the active outline) + `results/runs.csv`.
> Excluded: `CLAUDE.md`/`AGENTS.md`, `.claude/`, exemplars.
> **Checked against:** 15 PDFs in `papers/`, 15 keys in `thesis/references.bib`. Claims marked
> solid/thin were checked against the PDF text itself, not just titles.

---

## Headline

| Theme | Status | Blocker? |
|---|---|---|
| 1. SAR aerial person detection lineage | **Ready to write** | No |
| 2. Occlusion and vegetation | Writable, one thin spot | No |
| 3. Weitefeld failure baseline | **Ready to write** — publisher PDF now in, all numbers verified | No |
| 4. Detector architecture sweep (six arms) | **Not ready** — 5 of 6 unsourced | Yes |
| 5. State-space lineage + exclusion | Partial, **exclusion framing contradicted by own paper** | Yes — framing |
| 6. Open-vocabulary detectors | **Not ready** — zero sources | Yes |
| 7. Onboard VLMs | Partial — context solid, candidates unsourced | Partly |
| 8. Edge deployment / TensorRT | Thin but writable | No |
| 9. Multi-UAV coordination (O6) | **Missing entirely** | Decide scope |
| 10. SAR operational context | Thin | No |
| — Process-stage tooling (not LR) | Mostly missing, low priority | No |

**Write now (Day 3 plan):** themes 1 and 2. Theme 3 as soon as the proper Weitefeld PDF is in.
**Needs `/find-refs` first:** themes 4, 6, 7 (candidates), 5 (Mamba base paper), 9 (scope call).

---

## Three findings that change how you write, not just what you cite

### A. MambaNeXt-YOLO was deployed on Jetson Orin with TensorRT — the exclusion wording must change
The project docs justify dropping SSM detectors because they "may simply not deploy" and each
of three steps "can fail outright". But `mambanextyolo2025` itself reports benchmarks on
**Jetson Xavier NX and Jetson Orin, using TensorRT at FP16**, and says it was "successfully
deployed on resource-constrained edge devices". A marker who has read the paper you cite will
catch the contradiction.

What survives: the exclusion is a **project schedule-risk decision** (ARM64 build + possible
TensorRT plugin work would fall on you, with no guarantee inside a thesis timeline), not a
claim that Mamba detectors cannot run on Jetson. Also note the MambaNeXt-YOLO PDF never
names a `selective_scan` CUDA kernel explicitly — the custom-kernel dependency is better
sourced to the Mamba base paper (missing, see theme 5) and VMamba (`liu2024vmamba`, which does
discuss the selective scan and its CUDA implementation). Frame it as: *authors demonstrated
deployment; reproducing that path on this project's hardware and schedule was judged too
risky relative to the thesis contribution.*

### B. `pop_infrared2025` is an infrared dataset — thin support for an RGB claim
The progress report and outline use it for "detectors trained on unoccluded data fail on
occluded subjects". The paper does show exactly that — but in **thermal/infrared** imagery
(POP dataset, YOLOv8s). Your project is RGB. Keep it (it's the right finding), but either say
"in thermal imagery" when citing, or pair it with an RGB occlusion source.

### C. Weitefeld PDF — RESOLVED 2026-09-16
`weitefeld dataset.pdf` (the proper Scientific Data publisher copy) is now in `papers/`,
replacing the unreadable browser-print version. Every number the proposal leans on —
10,659 images, 34,424 anomaly labels, 405 findings, ~300 m altitude, 3–5 cm/px GSD, YOLOv12
at 0.016% average / 2.6% max confidence, 160 volunteers — verified word-for-word against the
extracted text. Safe to quote directly now.

**Watch for:** the paper describes two flight passes. An initial broad scan (25 km², 30,454
images, ~4×4 cm/px) is a *different* pass from the crowd-sourced priority zone (10,659 images,
3–5 cm/px GSD, 405 findings) that all the dataset numbers above come from. Don't conflate the
two — cite only the priority-zone figures.

---

## Theme-by-theme

### 1. SAR aerial person detection lineage — READY

**Solid**
- `ren2017fasterrcnn` — two-stage detector baseline. Solid for "classical two-stage anchor".
- `bozicstulic2019heridal` — HERIDAL; **45–60 m altitude confirmed** in text; 4000×3000 images.
- `yolo_sar_drones2025` (Ciccone & Ceruti) — YOLOv5s variants on HERIDAL; best 0.802 mAP@50
  (YOLOv5s-PBfpn-Deconv); Jetson Nano deployment.
- `botea2026lostperson` — YOLO11n/s for SAR person detection on custom data; weak mAP@50-95.
- `zhang2025aerialsurvey` — survey; four method categories (object-aware, sample-oriented,
  information-fusion, lightweight); names occlusion as a core challenge; VTSaR benchmark.

**Thin (caveats to carry into prose)**
- **HERIDAL's 88.9%** is a detection rate (recall) reported **alongside 34.8% precision**.
  Quoting 88.9% alone overstates performance. Either quote both or say "88.9% recall".
- **Ciccone's 0.802 mAP@50** is a YOLOv5s variant, not YOLO generically. Say which model.

**Missing (optional)**
- Comparison datasets named in docs but not held: VisDrone, SARD, WiSARD
  (`companion_computer_choices.md` gives WiSARD as arXiv 2309.04453 — verify). Optional; the
  survey already covers the dataset landscape.
- `companion_computer_choices.md` also lists Marušić et al. 2020, *Multimodel Deep Learning
  for Person Detection in Aerial Images* (Electronics) — same Split group as HERIDAL. Optional.

### 2. Occlusion and vegetation — WRITABLE, one thin spot

**Solid**
- `gui2026seeing` — RGB–thermal fusion for drone person detection under foliage; drone at
  **50–80 m, 45–60° oblique**; 6,125 RGBT pairs. Solid for "sensor-side answer to foliage",
  and the altitude is a useful extra differentiator (still well above 5–10 m).

**Thin**
- `pop_infrared2025` — see finding B (infrared, not RGB).

**Missing**
- **Synthetic occlusion / cutout / copy-paste augmentation** — your occlusion instrument
  (`occlude.py`: cutout, blobs, texture, foliage modes; distractor pasting) has no literature
  grounding. One source for occlusion-style augmentation is enough. Needed for Process §4.3
  more than the LR, but the LR's "synthetic occlusion as an evaluation instrument" point needs it.
- **An RGB occluded-person detection source** — to pair with (or replace) `pop_infrared2025`.

### 3. Weitefeld failure baseline — READY

**Solid (bib)** — `nathan2026weitefeld`, DOI verified.
**Solid (verification)** — see finding C. Publisher PDF now in `papers/`, all quoted numbers
checked against the source text.

Positioning is well supported by project docs and doesn't need new sources: ~300 m crewed
aircraft vs 5–10 m UAV; 4 coarse classes vs fine HPI taxonomy; crowd-sourced subjective labels
(plus your own finding: 281/1,909 person rows "excluded, not checked", `overfit_gates.md`).

### 4. Detector architecture sweep — NOT READY

**Solid** — none of the six arms have a source. (`botea2026lostperson` *uses* YOLO11 but isn't
the YOLO11 source.)

**Missing — required, one each**
| Arm / concept | Where named | Notes for `/find-refs` |
|---|---|---|
| YOLO11 | `training_nomenclature.md`, `phase_execution_guide.md` D4 | Ultralytics software; likely a software/docs citation, no formal paper — decide citation form |
| YOLO26 | `phase_execution_guide.md` P3.1/P3.2 | Docs cite arXiv **2510.09653** + Ultralytics docs — verify |
| YOLOv12 | same | area-based self-attention |
| YOLOv13 | same | Docs cite **Lei et al., arXiv 2506.17733** (HyperACE, FullPAD) — verify authors; not the same Lei as MambaNeXt-YOLO |
| RF-DETR | same | Roboflow, Apache-2.0 |
| RF100-VL | `objective_changes...`, P3.2 | The domain-transfer benchmark you justify RF-DETR with — needs its own source |
| D-FINE | same | distribution-refinement regression |

**Missing — optional**
- RT-DETR / DETR — only if you describe the DETR lineage the two transformer arms descend from.
- DEIM, RTMDet — named once each in the guide as comparison numbers; skip unless quoted.
- YOLOv8 — named as family member; skip.

### 5. State-space lineage and the exclusion — PARTIAL

**Solid**
- `zhu2024visionmamba` — bidirectional scan for images (ICML 2024).
- `liu2024vmamba` — SS2D four-way scan; linear complexity; discusses selective scan/CUDA.
- `mambanextyolo2025` — detection-adapted hybrid; **but see finding A**.

**Missing — required**
- **Mamba (Gu & Dao)** — the root of the lineage and the correct source for the selective-scan
  mechanism. Can't name the lineage without it.

### 6. Open-vocabulary detectors (D-arm) — NOT READY

**Solid** — none.

**Missing — required**
| Model | Where named |
|---|---|
| YOLO-World | `objective_changes...`, `technical_work_timeline.md`, flowchart, `dev_notes.md` |
| YOLOE | flowchart, P3.1, P3.5 |
| Grounding DINO | flowchart, P3.5, `dev_notes.md` |
| OWLv2 | `objective_changes...`, `technical_work_timeline.md` |

Prioritise **YOLO-World + one of Grounding DINO/OWLv2** if budget is tight — they're the two
distinct design points (YOLO-speed prompted detector vs grounding transformer). Note the docs
aren't consistent on the candidate list (OWLv2 appears in some docs, YOLOE/Grounding DINO in
others) — worth settling which ones you'll actually name.

**Missing — optional**
- An open-vocab-on-aerial/small-object evaluation source, if you want to argue robustness on
  tiny targets is uncertain (outline §2.5 flags this).
- CLIP — foundation both OVDs and VLMs build on; `chen2025gradnavpp` uses it. Optional.

### 7. Onboard VLMs — PARTIAL

**Solid**
- `sarwar2026benchmarking` — **five VLMs on Jetson Orin Nano 8 GB via Ollama/llama.cpp**,
  TTFT, throughput, power. Your exact hardware and runtime. Solid for edge-VLM benchmarking
  method. Its reference list cites SmolVLM deployment work (Baghel et al.) — a citation-chaining
  lead for the candidates below.
- `chu2024mobilevlmv2` — small-VLM design; discusses Jetson Orin, llama.cpp, quantisation.
  Solid for design context. Not one of your candidates.
- `chen2025gradnavpp` — **runs CLIP asynchronously with the policy on a Jetson Orin Nano**.
  Solid precedent for async vision-language processing on your hardware class.
- `williams2026litevlah` — VLA on **Jetson AGX Orin** (larger module); pre-fill dominates
  latency. Solid for the async-queue / throughput-not-per-frame argument.

**Positioning flag** — GRaD-Nav++ already does async onboard vision-language on an Orin
Nano, and Sarwar already benchmarks VLMs on the identical device. The async/on-device design
is therefore *not* novel on its own; your differentiator is the **role** (verification of
low-confidence detections and held-out HPI classes, in SAR, benchmarked against cheap
re-detection). Say that, don't imply the deployment pattern is new.

**Missing — required (the actual candidates)**
| Candidate | Where named | Notes |
|---|---|---|
| SmolVLM2 | `technical_work_timeline.md` Phase 4 | chain from Sarwar's refs |
| Qwen2-VL-2B | same | |
| Moondream2 | same | may only have a model card/GitHub, no paper — `/find-refs` to decide |
| Florence-2 | added 2026-09-16, `temp_papers/florence2_2311.06242.pdf` | CVPR 2024, 232M/771M params. Detection/grounding-native (unified prompt interface for captioning/detection/grounding), unlike the chat-repurposed VLMs above — a genuinely different design point worth naming in the critical comparison |
| InternVL2.5-1B/2B | added 2026-09-16, `temp_papers/internvl25_2412.05271.pdf` | arXiv 2412.05271, preprint only (no venue found). 1B/2B checkpoints part of this family paper's release |

**Missing — optional**
- LoRA — Phase 4 VLM fine-tuning (P3.3 says LoRA stays in scope there). Process/final thesis,
  not proposal LR, unless you commit to fine-tuning.

**No citation needed** — "a VLM relabelling a box the detector already classified adds latency
for no information" is your own design reasoning.

### 8. Edge deployment / TensorRT — THIN BUT WRITABLE

**Solid**
- `yolo_sar_drones2025` — body text: *no INT8 or TensorRT applied*, ~**450 ms/frame at
  640×640** on Jetson Nano (≈2.2 fps). Supports "unoptimised deployment is far below
  real-time". **Caveat:** the same paper's abstract claims "real-time inference on embedded
  hardware (Jetson Nano)" for its final model — cite the 450 ms as the *unoptimised,
  preliminary* measurement specifically, not as the paper's overall conclusion.

**Thin**
- The general claim "TensorRT + quantisation is what makes real-time plausible on Orin-class
  hardware" rests on one data point from older hardware (Jetson Nano, YOLOv5s).
  **Free strengthening already in your bib:** `mambanextyolo2025` reports TensorRT FP16
  benchmarks on Jetson Orin; `botea2026lostperson` describes Ultralytics' FP16/INT8/TensorRT
  export path. Together these cover the practice without new sources.
- `sarwar2026benchmarking` — covers quantised on-device VLM measurement methodology.

**Missing — optional lead**
- *Human Detection in UAV Thermal Imagery: Dataset Extension and Comparative Evaluation on
  Embedded Platforms* (MDPI Drones / PMC, 2025) — Jetson AGX Orin benchmarking, listed in
  `companion_computer_choices.md`. Unverified. Nice-to-have, not needed.
- NVIDIA TensorRT / JetPack documentation — software citation for Process §4.6 if the style
  guide wants tools cited.

### 9. Multi-UAV coordination (O6) — MISSING ENTIRELY

**Solid** — none. No paper in `papers/` covers cooperative multi-UAV search, coverage planning,
or detection sharing.

**Scope decision first:** O6 is described in your own docs as the first phase to cut. Options:
(a) one short LR paragraph with 1–2 sources on cooperative multi-UAV search/coverage, or
(b) no LR coverage, O6 justified only in Process. If O6 stays an objective in the proposal,
(a) is safer — an objective with zero literature grounding is an easy marker comment.

**Missing if (a)** — cooperative multi-UAV area coverage / search; ideally one SAR-specific.

### 10. SAR operational context — THIN

**Thin**
- `zhang2025aerialsurvey`'s introduction frames SaR as locating people "within critical time
  constraints" — enough for a one-line motivation. It's a survey's framing sentence, not
  evidence about search outcomes, so don't build an argument on it.

**Missing — optional**
- A lost-person behaviour / time-to-find source (outline §1 placeholder; `phase_execution_guide.md`
  mentions simulated lost persons). Only needed if the Introduction makes a stronger claim than
  "time-critical".

---

## Process-stage tooling (not Literature Review — track separately)

Named in project docs, no source held. Needed for Process/methods, mostly as software or
standards citations, not academic papers. Low priority for the LR pass.

| Item | Where named | Likely citation form |
|---|---|---|
| Ultralytics (YOLO11/12/26, TensorRT export) | many | software |
| SAHI (sliced inference) | `dev_notes.md`, P1.4, `how_model_training_works.md` | paper exists |
| Bootstrap CIs over placements | P3.7, flowchart | stats reference (optional) |
| Inter-annotator agreement (IAA / kappa) | P2.5 | stats reference |
| CASA sub-2 kg excluded RPA rules | P0.1, P2.1 | regulation — needed (rubric names standards) |
| COCO (pretraining) | many | paper exists |
| CVAT / Label Studio | P0.6 | software |
| ByteTrack / BoT-SORT | Phase 5 (time-permitting) | only if Phase 5 described |
| PX4, Gazebo Harmonic, ROS2 | sim docs | software |
| AdamW | `how_model_training_works.md` | only if optimiser discussed |

---

## Suggested `/find-refs` order

1. ~~Weitefeld publisher PDF~~ — done 2026-09-16, theme 3 unblocked.
2. **Mamba (Gu & Dao)** (unblocks theme 5 and fixes finding A's sourcing)
3. **Six-arm sources:** YOLO26, YOLOv12, YOLOv13, RF-DETR, RF100-VL, D-FINE, + YOLO11 citation form
4. **Open-vocab:** YOLO-World, Grounding DINO, OWLv2, YOLOE (settle which you'll name)
5. **VLM candidates:** SmolVLM2, Qwen2-VL, Moondream2 (chain from Sarwar's reference list), plus Florence-2 and InternVL2.5-1B/2B (sourced 2026-09-16, in `temp_papers/`)
6. **One RGB occlusion source** + **one occlusion-augmentation source**
7. **O6 scope call**, then 1–2 multi-UAV sources if keeping it in the LR
8. CASA regulation (for Process)

Items 2–5 are required for the proposal LR. 6 strengthens it. 7–8 depend on scope/section.
